% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer.R
\name{write_rsync_filter}
\alias{write_rsync_filter}
\title{Write an rsync filter file for copying pipeline outputs}
\usage{
write_rsync_filter(
  path = "gmaio_filter.txt",
  mode = c("all", "metagenome", "isolate")
)
}
\arguments{
\item{path}{Filter file to write.}

\item{mode}{\code{"all"}, \code{"metagenome"} or \code{"isolate"}.}
}
\value{
\code{path}, invisibly.
}
\description{
Use this to copy the small set of files gmaio needs from a pipeline results directory
(for example on a remote server) without the reads, BAMs, assemblies and bins.
Outputs the pipeline has not produced yet are skipped, so the copy can be rerun as a
run progresses; only new or changed files are transferred.
\code{\link[=fetch_pipeline_results]{fetch_pipeline_results()}} does the same from within R using \code{config.yml}.
}
