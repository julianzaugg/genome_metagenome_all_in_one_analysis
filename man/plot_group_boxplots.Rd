% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_tests.R
\name{plot_group_boxplots}
\alias{plot_group_boxplots}
\title{Boxplots of a value by group, one panel per measure}
\usage{
plot_group_boxplots(
  long.df,
  by,
  group,
  colours.v = NULL,
  pairwise_test = c("dunn", "wilcoxon"),
  p_adjust_method = "BH",
  show_test = TRUE,
  brackets = TRUE,
  bracket_alpha = 0.05,
  bracket_label = c("stars", "p"),
  bracket_size = 8,
  shape_by = NULL,
  shapes = NULL,
  box_width = 0.6,
  box_alpha = 0.5,
  point_size = 1.8,
  point_alpha = 1,
  jitter_width = 0.12,
  panel_labels = NULL,
  x_label = NULL,
  y_label = NULL,
  ncol = NULL,
  free_y = TRUE,
  x_text_angle = 30,
  base_size = 9,
  legend_position = "right"
)
}
\arguments{
\item{long.df}{Long data with \code{Value}, the \code{by} and \code{group} columns and any \code{shape_by} column.}

\item{by}{Column with one panel per level (e.g. \code{"Measure"}).}

\item{group}{Column on the x axis.}

\item{colours.v}{Named colours for \code{group}.}

\item{pairwise_test}{\code{"dunn"} (default, post-hoc after Kruskal-Wallis) or \code{"wilcoxon"},
for more than two groups.}

\item{p_adjust_method}{Correction for the pairwise tests within each panel.}

\item{show_test}{Print the panel test (Wilcoxon or Kruskal-Wallis p) above the boxes.}

\item{brackets}{Draw brackets between pairs with \code{P_adjusted < bracket_alpha} (more than two groups).}

\item{bracket_alpha}{Adjusted p value below which a pair gets a bracket.}

\item{bracket_label}{\code{"stars"} (\code{*}, \verb{**}, \verb{***}) or \code{"p"} (the adjusted p value).}

\item{bracket_size}{Font size of bracket labels, in points.}

\item{shape_by}{Optional column shown as point shape (a legend is added).}

\item{shapes}{Named point shapes for \code{shape_by}; filled shapes (21-25) take the group colour.}

\item{box_width, box_alpha}{Box width and fill transparency.}

\item{point_size, point_alpha, jitter_width}{Point size, transparency and horizontal jitter.}

\item{panel_labels}{Named panel titles, e.g. \code{c(Richness = "Observed genera")}.}

\item{x_label, y_label}{Axis titles.}

\item{ncol}{Number of panel columns.}

\item{free_y}{Give every panel its own y axis.}

\item{x_text_angle}{Angle of the group labels.}

\item{base_size}{Base font size, in points.}

\item{legend_position}{Legend position when \code{shape_by} is used.}
}
\value{
List with \code{plot}, \code{tests} (per panel) and \code{pairwise} (\code{NULL} for two groups).
}
\description{
Draws each group as a box with its samples as points, tests the groups in every panel
(\code{\link[=group_tests]{group_tests()}}) and, with more than two groups, marks the significant pairs from
\code{\link[=pairwise_tests]{pairwise_tests()}} with brackets. Used by \code{\link[=plot_alpha_diversity]{plot_alpha_diversity()}} and
\code{\link[=plot_nonpareil_metrics]{plot_nonpareil_metrics()}}, which pass their figure options on to this function.
}
