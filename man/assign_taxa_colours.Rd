% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{assign_taxa_colours}
\alias{assign_taxa_colours}
\title{Assign taxon colours}
\usage{
assign_taxa_colours(
  taxa.df,
  existing.v = NULL,
  fixed.v = NULL,
  scheme = c("distinct", "hierarchical")
)
}
\arguments{
\item{taxa.df}{Data frame with \code{Label}, \code{Phylum} and \code{Abundance} (used for ordering).}

\item{existing.v}{Named vector of saved colours (by label).}

\item{fixed.v}{Named vector of user-fixed colours (by label).}

\item{scheme}{\code{"distinct"} or \code{"hierarchical"}.}
}
\value{
Named character vector of colours by label.
}
\description{
Phyla get qualitative colours in order of abundance. With \code{scheme = "distinct"},
taxa below phylum get their own qualitative colours in order of overall abundance, so
the most abundant taxa get the most distinct colours. With \code{scheme = "hierarchical"},
they get shades of their phylum colour (clearer grouping, but similar shades when a
phylum has many taxa). Unassigned taxa are grey.
}
