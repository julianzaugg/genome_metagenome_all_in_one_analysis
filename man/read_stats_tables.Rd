% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_stats.R
\name{read_stats_tables}
\alias{read_stats_tables}
\title{Read statistics tables for reporting}
\usage{
read_stats_tables(read_stats.df, metadata.df = NULL, bases = TRUE)
}
\arguments{
\item{read_stats.df}{Pipeline read statistics (\code{project.l$read_stats}).}

\item{metadata.df}{Optional metadata; adds \code{Sample_label} after \code{Sample_ID}.}

\item{bases}{Include the base-level mapping columns in \code{Read_stats} (default for long reads;
for short reads of equal length they repeat the read percentages).}
}
\value{
Named list of data frames \code{Read_stats}, \code{Read_stats_full} (\code{NULL} when it would repeat
\code{Read_stats}) and \code{Column_descriptions} (\code{Sheet}, \code{Column}, \code{Description}), or \code{NULL} when
\code{read_stats.df} is \code{NULL}.
}
\description{
Turns the pipeline's \code{read_stat_report.tsv} into three sheets:
\code{Read_stats}, the read funnel (raw reads, each QC step, reads mapped to each genome set)
with, for long reads, one base-level column per genome set; \code{Read_stats_full}, the report
unchanged; and \code{Column_descriptions}, what each column means.
}
\details{
For each genome set the pipeline reports three base-level mapping metrics, A, B and C.
\code{Read_stats} keeps only A, renamed \verb{Bases_mapped_<set>_percent}: the full length of every
mapped read as a percentage of all sequenced bases, which is how much of the data a genome
set explains when read lengths vary (Nanopore). B (aligned bases only) and C (also counting
supplementary alignments) are usually within a fraction of a percent of A, and stay in
\code{Read_stats_full}.
}
