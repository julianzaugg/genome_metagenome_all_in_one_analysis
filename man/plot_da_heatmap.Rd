% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_abundance.R
\name{plot_da_heatmap}
\alias{plot_da_heatmap}
\title{Heatmap of differentially abundant features}
\usage{
plot_da_heatmap(
  profile,
  consensus.df,
  metadata.df,
  group,
  palettes.l = list(),
  min_methods = 1,
  max_features = 60,
  methods = c("MaAsLin3", "LinDA", "sPLS-DA"),
  rank_annotation = "Phylum",
  not_called_colour = "#F2F2F2",
  conflict_colour = "#D7191C",
  ...
)
}
\arguments{
\item{profile}{The \code{gm_profile} the differential abundance was run on.}

\item{consensus.df}{Result of \code{\link[=da_consensus]{da_consensus()}}.}

\item{metadata.df}{Metadata.}

\item{group}{Metadata column that was tested; used for the column split and the track colours.}

\item{palettes.l}{Project palettes (\code{project.l$palettes}).}

\item{min_methods}{Minimum number of methods calling a feature.}

\item{max_features}{Maximum number of features shown (most methods first).}

\item{methods}{Methods drawn as tracks.}

\item{rank_annotation}{Feature column drawn as the first track (e.g. \code{"Phylum"}), when present.}

\item{not_called_colour}{Track colour where a method did not call the feature.}

\item{conflict_colour}{Track colour for features called higher in different groups.}

\item{...}{Options passed to \code{\link[=plot_heatmap]{plot_heatmap()}}, overriding the defaults set here
(\code{column_split = group}, \code{row_split = "Higher_in"}).}
}
\value{
A \code{Heatmap}, or \code{NULL} when fewer than two features pass.
}
\description{
Abundance of every feature called by at least \code{min_methods} methods, with row tracks for
the group the feature is higher in, the group each method called it for (so disagreement
between methods shows as a colour mismatch) and features whose methods conflict in direction.
Built on \code{\link[=plot_heatmap]{plot_heatmap()}}; any of its options can be passed to change the figure.
}
