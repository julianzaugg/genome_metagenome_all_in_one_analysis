% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer.R
\name{check_inputs}
\alias{check_inputs}
\title{Report which pipeline outputs are available}
\usage{
check_inputs(config.l, verbose = TRUE)
}
\arguments{
\item{config.l}{A \code{gm_config}.}

\item{verbose}{Print the report.}
}
\value{
Tibble with \code{step}, \code{key}, \code{description}, \code{status} (\code{found}, \code{missing} or
\code{ambiguous}), \code{n_files} and \code{files}, invisibly.
}
\description{
Looks up every output gmaio reads for the project's mode, grouped by processing step,
and reports steps as complete, partial or not found.
Missing outputs are expected for partial runs or skipped pipeline steps; \code{main.R} skips
whatever is not there.
}
