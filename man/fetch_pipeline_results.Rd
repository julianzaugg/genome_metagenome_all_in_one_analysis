% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer.R
\name{fetch_pipeline_results}
\alias{fetch_pipeline_results}
\title{Copy pipeline outputs into the project with rsync}
\usage{
fetch_pipeline_results(
  config_path = "config.yml",
  source = NULL,
  dry_run = FALSE,
  rsync_args = character()
)
}
\arguments{
\item{config_path}{Path to \code{config.yml}.}

\item{source}{Override \code{pipeline_remote}.}

\item{dry_run}{List what would be copied without copying.}

\item{rsync_args}{Extra arguments passed to rsync, e.g. \code{"--progress"}.}
}
\value{
The \code{check_inputs()} table, invisibly (\code{NULL} for a dry run).
}
\description{
Copies the files gmaio reads from \code{pipeline_remote} (anything rsync accepts, such as
\verb{user@server:/path/to/results} or a mounted directory) into \code{pipeline_results}, then
reports what was found with \code{\link[=check_inputs]{check_inputs()}}.
Rerun it as the pipeline progresses; only new or changed files are transferred.
Remote sources need rsync on both machines and ssh access; if ssh asks for a password,
run the script from a terminal rather than RStudio.
}
