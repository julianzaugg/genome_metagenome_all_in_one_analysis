% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mags_within_sample.R
\name{within_sample_mag_summary}
\alias{within_sample_mag_summary}
\title{Per-sample MAG recovery summary}
\usage{
within_sample_mag_summary(project.l)
}
\arguments{
\item{project.l}{A processed \code{gm_project} with MAG results.}
}
\value{
List of two data frames:
\code{samples}, one row per sample with \code{Bins}, \code{HQ_bins} and, for within-sample runs,
\code{Own_representatives} and \code{Own_HQ_MAGs} (within-sample dereplicated genomes);
\code{mapping}, one row per sample and genome set with \code{Set}, \code{Genome_set} (a description),
\code{Scope} (\code{"Within sample"} or \code{"All samples"}), \code{Input_reads_mapped_percent},
\code{Reads_mapped_percent} and \code{Bases_mapped_percent}.
}
\description{
Summarises, for each sample, how many MAGs were recovered from it and how much of its
reads are explained by genome sets: its own MAGs (pipeline \code{--within_sample_dereplication},
sets \code{ws_derep_bins} and \code{ws_hq_bins}) next to the catalogue pooled across samples
(\code{hq_derep_bins} and the other cross-sample sets).
Comparing the two shows how much of a sample is captured by genomes assembled from that
sample alone versus genomes recovered anywhere in the study.
}
\details{
Mapping rates are reported against two different denominators:
\itemize{
\item \code{Input_reads_mapped_percent}: \% of the reads that went into mapping, i.e. after QC and
host removal, from CoverM's unmapped share. This is how much of the microbial part of a
sample the genomes explain, and is available as soon as CoverM has run.
\item \code{Reads_mapped_percent} and \code{Bases_mapped_percent}: \% of raw reads or raw sequenced bases,
from the pipeline read statistics report (written when the run finishes); bases use the
pipeline's metric A, better for Nanopore reads of very different lengths. These include
host and low-quality reads in the denominator, so they can be far lower when host removal
discards most reads.
}
}
