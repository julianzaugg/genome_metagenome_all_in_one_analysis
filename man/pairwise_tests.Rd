% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_tests.R
\name{pairwise_tests}
\alias{pairwise_tests}
\title{Pairwise tests between group levels}
\usage{
pairwise_tests(
  long.df,
  by,
  group,
  method = c("dunn", "wilcoxon"),
  p_adjust_method = "BH"
)
}
\arguments{
\item{long.df}{Long data with \code{Value}.}

\item{by}{Column defining separate test families (e.g. metric).}

\item{group}{Grouping column.}

\item{method}{\code{"dunn"} or \code{"wilcoxon"}.}

\item{p_adjust_method}{Correction within each family, see \code{\link[stats:p.adjust]{stats::p.adjust()}}.}
}
\value{
Data frame with one row per pair and family: \code{Group_1}, \code{Group_2}, \code{N_1}, \code{N_2},
\code{Mean_1}, \code{Mean_2}, \code{Median_1}, \code{Median_2}, \code{Test}, \code{Statistic} (z for Dunn, positive when
\code{Group_2} ranks higher; W for Wilcoxon),
\code{P_value}, \code{P_adjusted} and \code{Significance} (\verb{***} < 0.001, \verb{**} < 0.01, \code{*} < 0.05, else \code{ns}).
}
\description{
\code{method = "dunn"} is Dunn's test, the post-hoc test that follows Kruskal-Wallis: it uses the
ranks of all samples (with a tie correction), so it matches the omnibus test.
\code{method = "wilcoxon"} runs a separate Wilcoxon rank-sum test on each pair.
P values are adjusted within each level of \code{by}.
}
